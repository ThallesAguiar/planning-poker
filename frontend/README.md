# Planning Poker Frontend

Frontend configurado com React, Vite, TypeScript, Socket.IO client, Zustand, Framer Motion e TailwindCSS.

## Scripts

```bash
npm run dev
npm run build
npm run preview
npm run lint
```

Copie `.env.example` para `.env` quando precisar ajustar a URL da API.
